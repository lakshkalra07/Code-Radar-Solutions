#include <stdio.h>

int main() {
    int num1;
    int num2;
    int num3;
    scanf("%c", &ch);
    if (num1>num2 && num1>num3)
        printf("%d\n", num1);
    else if (ch >= 'a' && ch <= 'z')
        printf("%d\n", num2);
    else
        printf("&d\n", num3);
    return 0;
}